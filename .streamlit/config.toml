# POSCO 브랜드 톤(딥 블루) — posco_2과제 디자인 시스템과 동일 테마.
# 보는 사람의 다크모드 설정과 무관하게 항상 흰 배경 + 네이비 포인트로 렌더링.
[theme]
base = "light"
primaryColor = "#003C71"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#EEF3F8"
textColor = "#1A2B3C"
font = "sans serif"
